#include<stdio.h>
#include<conio.h>
int main()
{
    int n;
    printf("Enter numbers:");
    do
    {
        scanf("%d",&n);
        if(n%2!=0)
        {
           break;
        }

    }while(1);
}
